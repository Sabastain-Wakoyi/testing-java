public class Expressions {
    public static void main(StringRef[] args) {


        // expressions = operands & operators
        //operands = values, variables, numbers, quantity
        //operators = +, -, *, /, %

       // int friends = 10;
        //friends = friends / 3;
       // friends++;
       // System.out.println(friends);

        //if we want to see the reminder when dividing by an integer,
        //then we should use the data type "double"

        double friends = 10;

        friends = (double) friends / 3;

        System.out.println(friends);


    }
}
