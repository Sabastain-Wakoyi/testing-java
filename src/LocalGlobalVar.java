public class LocalGlobalVar {
    public static void main (String[] args){
        // local variables are declared inside a method
        // and are visible only to that method

        //global variables are declared outside a method, but within a class
        //and are visible to all parts of a class

        DiceRoller diceRoller = new DiceRoller();

    }
}
